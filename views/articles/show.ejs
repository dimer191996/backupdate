<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <title>Create Article</title>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-4"><%= article.title %></h1>
      ​<picture>
        <source srcset="<%= article.image %>" type="image/svg+xml" />
        <img
          src="<%= article.image %>"
          height="200"
          width="200"
          class="img-fluid img-thumbnail"
          alt="..."
        />
      </picture>

      <h5><%= article.description %></h5>
      <span class="badge btn-secondary text-uppercase">
        <%= article.category %>
      </span>
      <div><%= article.createdAt.toLocaleDateString() %></div>
      <a href="/" class="btn btn-secondary mb-4 mt-4 text-uppercase"
        >All Articles</a
      >
      <a
        href="/articles/edit/<%= article.id %> "
        class="btn btn-info text-white mb-4 mt-4 text-uppercase"
        >Edit</a
      >

      <p><%- article.sanitizedHTML %></p>
    </div>
  </body>
</html>
